version: "1.0"
services:
  neo4j:
    image: neo4j:5
    container_name: neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - ~/neo4j-data:/data
      - ~/neo4j-import:/var/lib/neo4j/import
    environment:
      - NEO4J_AUTH=neo4j/password
